# Overview

Simple c program to visualize music by a simple drag & drop mechanism

# Supported features

- Currently we only support .wav, .ogg, .mp3, .qoa, .xm, and .mod (basically anything raylib LoadWave function supports)
